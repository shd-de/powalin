package de.shd.basis.kotlin.ui.http

/**
 * Enthält nur Konstanten für HTTP-Header.
 *
 * @author Florian Steitz (fst)
 */
object HTTPHeaders {

    /**
     * Konstante für den HTTP-Header `content-type`, der mitteilt, welches Format der Nachrichtenrumpf (`Body`) eines Requests oder einer Response hat.
     */
    const val CONTENT_TYPE = "content-type"
}
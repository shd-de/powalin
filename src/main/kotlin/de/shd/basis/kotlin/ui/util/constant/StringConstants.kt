package de.shd.basis.kotlin.ui.util.constant

/**
 * Ein leerer String.
 *
 * @author Florian Steitz (fst)
 */
const val EMPTY_STRING = ""

/**
 * Ein Slash (`/`).
 *
 * @author Florian Steitz (fst)
 */
@Suppress("unused")
const val SLASH = "/"

/**
 * Ein Semikolon (`;`).
 *
 * @author Florian Steitz (fst)
 */
const val SEMICOLON = ";"

/**
 * Ein Bindestrich (`-`).
 *
 * @author Florian Steitz (fst)
 */
const val DASH = "-"

/**
 * Drei Auslassungspunkte (`...`).
 *
 * @author Marcel Ziganow (zim)
 */
const val ELLIPSIS = "..."